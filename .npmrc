# Allow lifecycle scripts for trusted packages used by this workspace.
# Prisma needs to build its query engine; NestJS core uses postinstall for swc bindings.
# Use `pnpm approve-builds` interactively to widen this list.
auto-install-peers=true
strict-peer-dependencies=false
